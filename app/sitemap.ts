import type { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap { return [{ url: "https://sir-brothers-llc.roadie-frodie.chatgpt.site", lastModified: new Date(), changeFrequency: "monthly", priority: 1 }]; }
